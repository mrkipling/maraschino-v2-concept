/** @jsx React.DOM */

/**
 * The container that renders the columns and modules.
 */

Maraschino.Container = React.createClass({
    render: function() {
        return (
            <div className="row">
            </div>
        );
    }
});
