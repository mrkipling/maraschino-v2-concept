/** @jsx React.DOM */

/**
 * @fileOverview Render the page.
 */

// Defined in base.js.
Maraschino.init();

React.render(
    <Maraschino.Container.Base />,
    document.getElementById('container')
);
